using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Controls;
using System.Windows.Media;

namespace GongSolutions.Wpf.DragDrop
{
  class Playground
  {
    // we want to get the ItemControls itempresenter that is bound to ItemsSource, and then see if originalsource traces back to a child...
    // check if its a headereditemcontrol, make sure we're in the header
    public void x()
    {
      ItemsControl ic = new ItemsControl();
      // find ItemsPresenter
      //
    }
    
    
  }
}
